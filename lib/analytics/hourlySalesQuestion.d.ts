// Type declarations for the runtime helper lib/analytics/hourlySalesQuestion.js.
// Adjacent to the .js so the import
//   import { isUnsupportedHourlySalesQuestion } from "../../../lib/analytics/hourlySalesQuestion.js";
// resolves under strict TypeScript without allowJs.

export function isUnsupportedHourlySalesQuestion(question: string): boolean;
